import { Wallet } from '$lib/plugins/Wallet';
import { Alchemy } from '$lib/plugins/providers';
import { Ethereum, Solana } from '$lib/plugins/blockchains';

const alchemy = new Alchemy();
const ethereum = new Ethereum(alchemy);
const solana = new Solana(alchemy);

const wallet = new Wallet([alchemy], [ethereum, solana]);
wallet.getBalance('0x123...');
wallet.sendTransaction({...});
wallet.switchProvider();
